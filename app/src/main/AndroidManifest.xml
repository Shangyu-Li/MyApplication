<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dan.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".history_win.HistoryWin"/>
        <activity android:name=".execute_win.ExecuteWin"/>
        <activity android:name=".display_win.DisplayWin"/>
        <activity android:name=".display_win.SetDetail"/>
        <activity android:name=".display_win.SetWin"/>

        <provider android:name=".SQLiteDataBase.MenuProvider"
            android:authorities="com.example.dan.shop"/>
    </application>

</manifest>